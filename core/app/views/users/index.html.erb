<h1><%= if current_user
    current_user.login
  else
    "Not logged in!"
  end
 %></h1>

<% require 'pp'
pp self.current_user %>


<% str = ""
  self.methods.sort.each {|m| str += m.to_s + "<br>"}
  %>

<%= str %>


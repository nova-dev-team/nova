Huang Gang's scripts are located on the NFS server. Core is expected to
call those scripts.

In order to invoke HG's scripts, core need to mount NFS folders to
/config & /share.

// TODO add more detail

